{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=@m2/admin...",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/admin packages"
}
