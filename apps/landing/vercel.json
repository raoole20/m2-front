{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=@m2/landing...",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/landing packages"
}
